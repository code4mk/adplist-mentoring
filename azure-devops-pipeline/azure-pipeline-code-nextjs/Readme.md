this is adp list azure devops demo

# remote 
```bash
git remote add origin https://code4mk@dev.azure.com/code4mk/adp-azure-devops/_git/adp-azure-devops
git branch -M master
git add .
git commit -am "adp list demo"
git push -u origin master
```

# env

```bash
NETLIFY_AUTH_TOKEN=""
NETLIFY_SITE_ID=""
```